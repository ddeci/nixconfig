# Tailscale

Enrolls every peer in this Clan service with one reusable Tailscale auth key stored by Clan vars. Local Tailscale behavior remains configurable through the upstream `services.tailscale` NixOS options.
